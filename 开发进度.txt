Sw 编译器（swcc）开发进度
==========================

更新：2026-08-14（main）

一、已完成

1. 项目基建
   - Rust workspace：sw_common / sw_frontend / sw_semantic / sw_codegen_cranelift / swc
   - 私有仓库：Gitee https://gitee.com/YJTXJH/swcc.git（main）
   - 规范文档：docs/00~07、docs/grammar/sw.ebnf

2. 前端（纯 Rust，无外部依赖）
   - 词法器 / 语法分析器 / AST；JS 风格语法（let/const、function、class、
     箭头函数、模板字符串、import/export）

3. 语义层
   - 名称解析、类型检查、MIR 生成；import 模块系统

4. 后端（Cranelift 进程内代码生成，零 clang 依赖）
   - MIR → 机器码 → COFF/ELF/Mach-O 对象 → lld 链接 → 可执行文件
   - 三元表达式、异常（自包含 setjmp/longjmp，非 CRT）、闭包（值捕获+间接调用）
   - 数组、类、for-of、模板字符串、复合赋值

5. 跨平台与分发
   - 按 triple 编译：Windows / Linux / macOS × x86_64 / aarch64
   - Linux x86_64 / aarch64 musl 静态链接（aarch64 用 rustup compiler-builtins
     补齐 128 位软浮点辅助函数）
   - macOS 原生链接（系统 cc 编译运行时并链接，Mach-O 符号自动加下划线）
   - Windows SDK 打包：swc + lld + MinGW UCRT 库 + 预编译运行时，解压即用
     （dist/swc-windows-x64.zip，已验证无外部工具链可构建运行）

6. CI（.github/workflows/ci.yml，仓库在 Gitee，需镜像到 GitHub 触发）
   - Windows / Ubuntu / macOS 三平台矩阵，自动下载 llvm-mingw 20260616
   - cargo fmt 检查 + 全量测试 + release 构建 + 三平台冒烟 + SDK artifact

7. 表达式与后端补齐（本批）
   - struct 值语义全链路：字面量、参数按地址传（被调方复制）、sret 返回值、
     字段读写、按值拷贝（`let b = a` 后互不影响）
   - `**` 幂运算：整数快速幂、浮点走 libm pow
   - `++/--` 表达式：前缀返回新值、后缀返回旧值
   - 赋值表达式：`a = b` 整体返回被赋的值（含复合赋值）
   - 后端浮点支持补齐：局部/参数/返回值、加减乘除、比较、三元、`%`（fmod）、
     自增、float 转字符串
   - 防护：struct 嵌套字段/struct 数组/类字段存 struct 明确报错，避免按值语义被破坏

8.（上一批结束时）测试：37 项全过（词法 10 / 语法 12 / 语义 8 / 代码生成 7），示例端到端验证

9. 本批（CLI / 标准库 / 泛型 / 内存管理）
   - CLI：`swc help`、`--version`、构建耗时输出（构建成功时打印用时 ms）
   - 标准库：`std/io`（print/println/read_line）、`std/math`（abs/min/max/floor/ceil/sqrt）、
     `std/fs`（open/close/write/read_all）、`std/string`（index_of/contains/starts_with/substring）
   - 泛型函数端到端：调用点单态化（推断类型实参、生成并缓存专用实例、支持自递归），
     类型替换覆盖局部/转换/数组/结构体；模板函数本身不再生成 MIR
   - 内存管理：运行时保守式标记-清除 GC（原生栈 + 寄存器溢出区 + 程序数据段 + 堆内引用扫描），
     字符串/数组/class/闭包统一走 GC 分配器，按阈值自动回收；30 万次分配压力测试通过
   - 顺手修复存量 bug：调用结果类型未登记（`f().length` 解析失败）、字符串 `.length` 偏移、
     字符串 for-of 元素类型、成员链 span 碰撞导致的类型误判

10.（本批后）测试：38 项全过（词法 10 / 语法 12 / 语义 8 / 代码生成 8）

11. 发布 0.1.0（本批）
   - swc 支持 Linux 解压即用 SDK 布局：<root>/musl/<triple> 自带 crt/libc/compiler-builtins，
     <root>/lib 自带 x86_64/aarch64 预编译运行时对象，无需 clang 即可静态链接
   - 打包脚本：tools/make-sdk.py（Windows）、tools/make-sdk-linux.py（Linux tar.gz）、
     tools/make-sdk-macos.py（macOS zip）
   - .github/workflows/release.yml：打 `v*` 标签自动三平台打包并发布 GitHub Release
   - 本地已验证：假 Linux SDK 布局（自带 musl + 预编译运行时）x64/aarch64 静态链接均成功

12. 已镜像：https://github.com/3219720339/swcc（main + v0.1.0 标签已推送），
    GitHub Actions 自动验证三平台 CI 与 macOS 真机（GC dyld 路径）并出 0.1.0 Release

13. 0.1.0 发布完成（2026-08-14）：
    - CI 三平台全绿（Windows/Ubuntu/macOS 真机 smoke，含 GC 在 Apple Silicon 上运行验证）
    - v0.1.0 Release 已发布：swc-windows-x64.zip（33.9MB）、
      swc-linux-x64.tar.gz（8.2MB）、swc-macos.zip（2.2MB）
    - 过程中修复：cranelift x86 feature（arm64 主机交叉目标）、Mach-O 双下划线符号、
      macOS stdin/stdout 符号（___stdinp/___stdoutp）、Mach-O arm64 非法 text-relocation
    （开启 is_pic 走 GOT）、Windows runner 中文 print 编码崩溃、smoke 缺 build-check 目录

14. 语言完整度 + 标准库批次（2026-08-14）
    2026-08-14 07:20  环境变量：swc 加入用户 PATH、SW_TOOLCHAIN、SW_STDLIB（新终端生效）
    2026-08-14 07:22  修复任意目录执行：runtime 源目录改为 SW_RUNTIME/当前目录/可执行文件附近查找
    2026-08-14 07:25  可选链 ?. 空值短路（字段/索引/方法调用），可空赋值规则补齐（值→可空）
    2026-08-14 07:30  struct 嵌套：字段内联展平（偏移表+递归大小），深拷贝值语义，嵌套字面量目标类型传播
    2026-08-14 07:35  修复成员链 span 碰撞：字段解析改为"对象类型+字段名"直查，不再依赖 span 表
    2026-08-14 07:42  接口 vtable：implements 校验、类→接口赋值、全局接口槽位布局、类对象 vtable 头、
                       经 vtable 间接派发；顺带修复 float→int 显式转换按值类型判断
    2026-08-14 07:51  标准库：std/json（最小 JSON 解析）、std/time（now_ms/sleep_ms）、
                       std/unicode（utf8_len/utf8_char_at/utf8_substring）

    2026-08-14 07:58  泛型 struct/class：TypeResolver 改持可变类型表，惰性实例化并缓存；
                       泛型类实例的方法按实例 id 生成（this_class + 类型实参替换）；
                       泛型类暂不支持 implements 接口（显式报错）；测试与示例 probe-generic

    2026-08-14 08:05  文本地基修复：字符串 ==/!= 按内容比较、s[i] 按字符（UTF-8 码点）、
                       string.length 按字符数、字符串 for-of 按字符；表达式类型表改 (start,end) 复合键，
                       消除嵌套调用链的 span 碰撞
    2026-08-14 08:08  字符串链式方法：to_upper/to_lower/trim/replace/split/join/parse_int/parse_float/
                       contains/index_of/starts_with/substring（string[].join 支持 split().join() 链）
    2026-08-14 08:10  文件补齐：read_line_from/seek/file_size/exists/path_join；std/fs 与 std/string 声明更新；
                       测试与示例 probe-string

    2026-08-14 08:15  标准库注释：std/io、std/math、std/fs、std/string、std/json、std/time、
                       std/unicode 全部补模块说明 + 每个函数用途/参数/返回值/示例
    2026-08-14 08:18  进程基础：main(args: string[])、运行时入口构建 argv（Windows 走
                       CommandLineToArgvW+UTF-8，POSIX 用 CRT 参数）、std/os（getenv/exit）、
                       swc run 透传命令行参数；main 签名校验（0/1 个 string[] 参数、返回 int）

    2026-08-14 08:22  文件便捷封装：read_lines(path)（按行读取，去行尾换行/末尾空行）、
                       write_all(path, text)（覆盖写）、append(path, text)（追加）
    2026-08-14 08:25  错误处理增强：is_number 严格数字校验、parse_int_or/parse_float_or
                       （失败返回显式 fallback，不再静默 0）；文本补充 repeat/from_code_point；
                       时间补充 now_sec/date_string（YYYY-MM-DD）；链式方法表同步扩展；
                       测试与示例 probe-io

    2026-08-14 08:28  文件/目录系统：list_dir（Windows FindFirstFileA / POSIX readdir）、
                       is_dir、mkdir（Windows CreateDirectoryA / POSIX asm 别名 libc）、
                       copy_file、path_basename/path_dirname/path_ext
    2026-08-14 08:31  修复 MinGW ucrt 的 rename/remove 不可用问题：改用 Win32
                       MoveFileA/DeleteFileA（POSIX 用 asm 标签别名调 libc）；
                       测试与示例 probe-dir

    2026-08-14 08:35  字符级字符串操作：reverse（按码点反转）、index_of_char（返回字符序号）、
                       split_chars、substring_chars（链式方法）
    2026-08-14 08:38  格式化输出：pad_left/pad_right（按字符宽度填充）、
                       format_int（补零/右对齐）、format_float（小数位精度）
    2026-08-14 08:40  随机数与数学：rand_int（随机种子化）、clamp/gcd/lcm；
                       时间完整化：datetime_string（YYYY-MM-DD HH:MM:SS）、parse_date（本地时区往返）
    2026-08-14 08:41  修复时间函数 Windows 分支：date_string/datetime_string 按传入秒数换算，
                       parse_date 用 TzSpecificLocalTimeToSystemTime 保证本地时区往返一致；
                       测试与示例 probe-format

13. 本批（import 新形式 + 目录遍历 + u8[] 二进制 + format varargs）
    2026-08-14 09:16  import 新形式两种：
                       - `import * from "path"`：通配导入，把模块全部导出展开进当前作用域；
                       - `import "path"`：副作用导入升级为文件名命名空间（如 calc.add()）；
                       修存量 bug：Namespace 先占位 alloc_symbol 导致绑定到 Error 全局符号，
                       改为先取 SymbolId 再 push
    2026-08-14 09:16  递归目录遍历：std/fs 新增 walk_files/walk_dirs（完整路径、含子目录）；
                       修复两个 C 侧 bug：
                       - sw_find_data 缺 14 字节 cAlternateFileName，FindFirstFileA 写越界
                         16 字节破坏调用方栈（list_dir 返回后 *slot 变垃圾值）；
                       - sw_walk_impl 扩容后仅更新局部 array，上层仍指向旧数组；
                         改为传 sw_array** 指针回写
    2026-08-14 09:16  u8[] 紧凑布局 + 二进制 IO：sw_array_new 真正使用 elem_size（u8 步长 1、
                       其余 8）；后端数组读写按元素类型选择 1/8 字节步长；
                       std/fs 新增 read_file_bytes/write_file_bytes；
                       修模板插值 bug：整数类型误走 float_to_string，改用 int_to_string
    2026-08-14 09:16  printf 风格 format（varargs，语言特性级）：
                       - 新增 `...args: any` 可变参数语法（parser 已解析 rest，语义层补齐
                         ParamSig.rest 标记 + Type::Any + is_assignable 万能匹配）；
                       - 调用点把可变实参打包成「类型标签 + 值」数组（MirExpr::VarArgs），
                         标签：0=int / 1=float / 2=string / 3=bool / 4=char；
                       - std/string 新增 format(fmt, ...args)，支持 %d/%i/%u/%x/%X/%o/
                         %f/%e/%g/%s/%c/%% 与宽度精度（%5d、%.2f、%-10s、%08x）；
                       - 少传参数用 0/空串补齐，多传忽略；缓冲区按需扩容（GC 安全）
    2026-08-14 09:16  测试：48 项全过（词法 10 / 语法 12 / 语义 17 / 代码生成 9），
                       新增 checks_varargs_format_and_new_import_forms（校验 VarArgs MIR 节点）；
                       examples 全部回归通过（fib=0、probe-exc=42 等退出码不变）
    2026-08-14 09:16  跨平台验证：runtime.c 交叉编译 Linux x86_64/aarch64、macOS aarch64 通过；
                       aarch64-unknown-linux-musl 端到端链接（含 format 代码）成功
    2026-08-14 09:40  examples 全量回归（29 个文件）：
                       - 全部 swc check + swc run 通过；退出码与源码 return 逐一核对
                         （0/1/3/10/42/43/77 均为探针设计值）；
                       - 修复回归：probe-argv 编译失败（`home ?? "(none)"` 被误判类型），
                         根因是本批给 FnLower::expr_type 新增的 Binary 分支把 `??` 短路成
                         左操作数类型 string?，模板插值误走 float_to_string；
                         check_expr 本就普遍记录表达式类型，删除多余分支后恢复，
                         varargs 类型标签仍正确
    2026-08-14 09:40  CI 增强：新增 tools/run-examples.py（三平台逐个编译运行全部 examples，
                       断言预期退出码，超时 120s），接入 ci.yml 的 Windows/Linux/macOS smoke
                       （保留原有 aarch64/musl 交叉编译与 Mach-O/ELF 校验）
    2026-08-14 09:44  三平台 CI 失败定位与修复（Linux 4 探针退出码异常 + macOS 链接失败）：
                       - Linux probe-exc-*：x64 runtime.s 按 Windows 调用约定取参（rcx/rdx），
                         SysV ABI 下第一个参数在 rdi → sw_setjmp 把寄存器写进野地址，
                         进 try 即崩溃（swc run 对信号退出按 1 上报，掩盖成"退出码 1"）。
                         改 runtime.s → runtime_x64.S，用预处理宏按 _WIN32 选寄存器：
                         Windows 用 rcx/rdx，ELF/Mach-O 用 rdi/rsi；反汇编验证两边正确
                       - Linux/macOS probe-dir：非 Windows 分支用 __asm__ 把 libc 符号
                         mkdir/rename/remove 改名，与本文件同名导出包装函数撞符号：
                         ELF 上可执行文件定义遮蔽 libc，包装函数无限递归栈溢出；
                         Mach-O 上 C 符号带下划线前缀，引用 mkdir（无下划线）直接链接失败。
                         统一改用 mkdirat/renameat/unlink/rmdir（POSIX，三平台都有、
                         与包装函数名不冲突）；Apple 下 asm 符号补下划线前缀
                       - localtime_r 声明为返回 int 与实际指针返回不符（Linux 编译警告）：
                         改为 void*，两处 date/datetime 的 NULL 判断恢复正确
                       - 验证：runtime 交叉编译 6 目标（Windows/Linux/macOS × x86_64/aarch64）
                         通过；Windows 本机 29 个 examples 全部回归通过；cargo test 48 全绿；
                         Linux x86_64 musl 端到端链接 probe-exceptions/probe-dir/fib 成功，
                         nm 确认最终可执行文件内 mkdir/rename/remove 仅包装函数一个定义、
                         mkdirat/renameat/unlink/rmdir 已解析进 libc.a，无递归
    2026-08-14 10:09  probe-dir Linux 崩溃根因（CI trace 定位）：
                       - 逐步写文件探针 dir-debug 确认：mkdir/write_all 正常，
                         list_dir 内崩溃；
                       - 反汇编 musl 静态二进制发现符号劫持：标准库 extern 与 libc
                         同名（open/close/write/abs/mkdir/rename/remove/getenv），
                         静态链接下 musl 内部调用被解析到 Sw 包装函数：
                         musl opendir 内部调用 open() 被 Sw 的
                         open(sw_string*, sw_string*) 接住，(char*, int) 参数当
                         sw_string* 解引用野指针 → SIGSEGV；closedir 调 close 同样被劫持
                       - 修复：codegen 新增 extern_c_symbol 映射表
                         （open→sw_open、close→sw_close、write→sw_write、abs→sw_abs、
                         mkdir→sw_mkdir、rename→sw_rename、remove→sw_remove、
                         getenv→sw_getenv），runtime.c 对应改名；libc 同名符号恢复由
                         libc 提供，musl 内部调用不再被劫持
                       - 验证：Windows 29 个 examples 全过、cargo test 48 全绿；
                         x86_64/aarch64 musl 端到端链接通过；反汇编确认 opendir→libc
                         open、closedir→libc close、Sw 调用→sw_open/sw_close 等
                       - 顺带发现：模板字符串内 \n 转义未处理（普通字符串正常），
                         已列入待办

    2026-08-14 10:15  修复模板字符串转义（语言级 bug，此前待办）：
                        - 根因：lex_template_text 逐字符复制文本，完全不处理 `\`，
                          而普通字符串 lex_string 走 parse_escape；
                        - 修复：模板文本复用 parse_escape + flush_raw_bytes，
                          \n/\t/\r/\0/\" /\'/转义反引号/\\/\xNN/\u{...} 与普通字符串
                          完全一致；真实换行符仍按规范原样保留（模板可跨行）；
                          \$ 等非法转义按规范报错
                        - 测试：词法新增 3 项（模板转义、插值间转义、非法转义报错，
                          词法 10→13，全量 48→51 全绿）；新增端到端示例
                          examples/probe-template-escape.sw（已注册进
                          tools/run-examples.py，退出码 0）；13 个含模板示例全量回归通过

    2026-08-14 20:00  打包版本化 + 0.1.1 构建：
                       - workspace 版本 0.1.0→0.1.1；swc --version / help 改为读
                         CARGO_PKG_VERSION（不再写死）
                       - make-sdk.py / make-sdk-linux.py / make-sdk-macos.py：
                         产物名带版本号（swc-windows-x64-<版本号>.zip、
                         swc-linux-x64-<版本号>.tar.gz、swc-macos-<版本号>.zip），
                         SDK 内写 version.txt，版本号统一从根 Cargo.toml 读取
                       - 新增 tools/make-sdk.ps1（本机无 python 时的 Windows SDK
                         打包脚本；纯 ASCII 兼容 PowerShell 5.1，不删除既有文件）
                       - ci.yml Windows SDK 上传改通配 dist/swc-windows-x64-*.zip
                       - 修复打包缺口：SDK 缺 libshell32.a（链接参数 -lshell32 需要，
                         runtime main 的 CommandLineToArgvW 来自 shell32），
                         旧 0.1.0 zip 在 Windows 上无法链接任何程序
                       - 验证：dist/swc-windows-x64-0.1.1.zip（约 34MB）解压后无外部
                         工具链端到端编译运行 fib/probe-argv/probe-dir/probe-exceptions
                         全部通过；--version 输出 swc 0.1.1

    2026-08-14 20:10  进程交互标准库（std/os 新增 8 个函数，纯运行时）：
                       - platform()：windows/linux/macos
                       - run(cmd, args)：执行到结束，返回 stdout+stderr 合并文本
                       - run_with_input(cmd, args, input)：先写 stdin 再捕获输出
                       - run_status(cmd, args)：退出码（输出继承父进程）
                       - spawn(cmd, args)：后台启动返回 pid（失败 0）
                       - wait(pid)：阻塞等待并回收，返回退出码
                       - poll(pid)：非阻塞，退出码 / -1 运行中 / -2 未知
                       - kill(pid)：强制终止（成功 0）
                       - Windows 用 CreateProcessA + 管道（STARTF_USESTDHANDLES，
                         命令行自动按空格/引号转义）；POSIX 用 fork + execvp + 管道；
                         run_with_input 的 input 建议 < 管道缓冲（Windows 4KB），
                         过大且子进程不及时读会阻塞
                       - codegen extern_c_symbol 追加 8 个映射（sw_spawn/sw_wait/
                         sw_poll/sw_kill/sw_run/sw_run_with_input/sw_run_status/
                         sw_platform），避免静态链接遮蔽 libc 的 wait/kill/poll
                       - 示例 examples/probe-process.sw（三平台分支：cmd/sh、more/cat、
                         ping/sleep、true），31 个 examples 全过；cargo test 51 全绿；
                         runtime.c 六目标交叉编译通过，musl x86_64/aarch64 端到端链接
                         通过，nm 确认 libc wait/kill/poll 未被遮蔽

    2026-08-14 20:30  标准库大批量扩充（批次 A+B，约 60 个函数）：
                       - 新模块 std/encoding：base64_encode/decode、hex_encode/decode、
                         url_encode/decode（6）
                       - std/string +13：ends_with/trim_left/trim_right/lines/
                         split_whitespace/count/last_index_of/chars/from_utf8_bytes/
                         to_utf8_bytes/is_ascii/escape/unescape
                         （前 11 个注册进链式方法表）
                       - std/math +22：round/trunc/sign/sin/cos/tan/asin/acos/atan/
                         atan2/exp/log/log2/log10/hypot/cbrt/fmin/fmax/rand_float/
                         rand_range/pi/e（三角/对数直连 libm；
                         sign/rand_float/rand_range/pi/e 运行时实现）
                       - std/time +3：time_format（strftime 子集）/
                         time_from_parts/timezone_offset_sec
                       - std/io +3：eprintln/eprint（stderr）/read_all_stdin
                       - std/os +8：cwd/chdir/temp_dir/home_dir/hostname/cpu_count/
                         env_keys/setenv
                       - std/fs +8：file_size_path/file_mtime/is_file/chmod/touch/
                         copy_dir（递归）/remove_all（递归删除，已标注危险）/
                         glob（* 和 ? 通配，返回完整路径）
                       - codegen extern_c_symbol 追加 45 个 sw_ 映射；
                         string_method 追加 11 个链式方法
                       - 顺手修复三个存量 bug：
                         Windows exists() 对目录返回 0（fopen 打不开目录，改 GetFileAttributesA）；
                         Windows remove 不能删目录（补 RemoveDirectoryA）；
                         Windows chmod 写位判断错误（POSIX 0o200=128，MSVC _S_IWRITE=0x80，
                         误用 0x200 导致 chmod(420) 把文件设只读、后续删除失败）
                       - 新增 examples/probe-stdlib.sw（约 90 项断言覆盖全部新函数），
                         32 个 examples 全过；cargo test 51 全绿；runtime.c 六目标交叉
                         编译通过；musl x86_64/aarch64 端到端链接通过，
                         nm 确认 libc chmod/chdir/setenv/stat/sin/log2 未被遮蔽

    2026-08-14 20:40  测试整理：临时探针归入 examples（按约定，后续测试文件统一放 examples/）：
                       - 新增 examples/probe-chmod.sw：chmod 0o644/0o400（只读）/
                         0o777（恢复可写）/touch/remove/exists 跨平台断言，
                         注册进 tools/run-examples.py（退出码 0）
                       - 清理确认过的测试产物：build-check/、dir-demo/、walk-test/
                       - 验证：Windows 本机通过且无残留；probe-chmod 与 probe-stdlib
                         musl x86_64/aarch64 交叉链接通过，nm 确认 sw_chmod/sw_remove/
                         sw_touch 无遮蔽

    2026-08-14 20:50  macOS is_file 修复 + 编译缓存提速：
                       - macOS CI 失败点：probe-stdlib 的 is_file。根因：stat 结构体
                         偏移按平台区分时条件写错（写进 _WIN32 分支，而 Windows 根本
                         不走 stat；macOS 需要 mode=4/mtime=48），导致 macOS 用了
                         Linux 偏移（24/88），mode 读到 uid 上，is_file 恒返回 0。
                         改为 __APPLE__ 条件
                       - 编译慢分析（实测）：冷编译 runtime.c（-O2）约 0.6~1.0s
                         （CI macOS 首编 1.7s）；热缓存约 65ms；git 检出/切分支只改
                         mtime 不改内容也会触发整段重编（实测 571ms）
                       - 新增内容哈希缓存：compile_if_stale 增加 .hash 侧车文件
                         （FNV-1a 内容哈希 + 编译参数），mtime 判定为旧但内容一致时
                         直接复用对象；实测 mtime 假变从 571ms 降到 73ms

    2026-08-14 21:32  标准库扩充（批次 A+B，约 39 个函数）：
                       - std/os +12：desktop/documents/downloads/pictures/music/videos/
                         config/system 目录、username、pid、arch、unsetenv
                         （Windows SHGetKnownFolderPath + 失败回退主目录；
                         POSIX XDG 环境变量 + $HOME 回退）
                       - std/fs +11：path_absolute/path_normalize/is_absolute/
                         path_parts/expand_home/mkdir_p/disk_free/disk_total/
                         is_symlink/read_symlink/file_mode
                         （Windows 符号链接用 DeviceIoControl 读重解析点）
                       - std/string +4：is_empty/utf8_is_valid/truncate/ellipsis
                         （全部进链式方法表）
                       - std/math +5：deg_to_rad/rad_to_deg/is_nan/is_infinite/tau
                       - std/time +1：parse_datetime（"YYYY-MM-DD[ T]HH:MM:SS"）
                       - std/encoding +3：base64url_encode/decode（无填充）、html_escape
                       - std/json +3：json_stringify（紧凑序列化）/json_object_keys/
                         json_type_name
                       - codegen extern_c_symbol 追加 36 个 sw_ 映射；
                         string_method 追加 4 个；Windows 链接补 -lole32（CoTaskMemFree）
                       - 修复：videos_dir 在无该文件夹的机器上返回空（SHGetKnownFolderPath
                         报 0x80070002），改为回退到主目录/文件夹名
                       - 新增 examples/probe-stdlib2.sw（约 68 项断言），
                         34 个 examples 全过；cargo test 51 全绿；runtime.c 六目标交叉
                         编译通过；musl x86_64/aarch64 端到端链接通过，
                         nm 确认 statvfs/lstat/readlink/unsetenv 未被遮蔽

    2026-08-14 21:45  容器 std/array + std/map + `+` 字符串拼接放开：
                       - 语言（用法糖）：`+` 拼接放开——string + int/float/bool/char
                         （及反向 42 + "!"）自动转字符串拼接（JS 风格）；
                         check_binary 类型检查 + MIR 降级插入 int/float/bool/char_
                         to_string 再 string_concat
                       - std/array（新模块，13 个类型化函数）：sort_int/float/string、
                         reverse_int/float/string、min/max/sum_int、min/max/sum_float、
                         unique_string（原地排序/反转，qsort；空数组 min/max 返回 0）
                       - std/map（新模块，7 个函数）：map_new/set/get/has/remove/len/keys，
                         字符串键值、GC 管理、保持插入顺序
                       - 顺手修后端存量 bug：浮点数组此前从未验证——数组字面量存浮点
                         没做 bitcast（float→i64 槽）、索引读写没转回 f64；
                         给 MirExpr::Index/MirTarget::Index 增加 elem 类型字段，
                         读写按 f64 位模式 bitcast（cranelift 0.134 的 bitcast
                         需要 MemFlags 参数）
                       - codegen extern_c_symbol 追加 20 个 sw_ 映射
                       - 新增 examples/probe-stdlib3.sw（39 项断言），35 个 examples
                         全过；cargo test 51 全绿；runtime.c 六目标交叉编译通过；
                         musl x86_64/aarch64 端到端链接通过，nm 确认 qsort 等无遮蔽

    2026-08-14 21:55  函数级裁剪（gc-sections）：
                       - runtime.c 编译加 -ffunction-sections -fdata-sections；
                         Windows/Linux（lld）链接加 --gc-sections、
                         macOS（cc/ld64）加 -Wl,-dead_strip；
                         make-sdk.py / make-sdk.ps1 / make-sdk-linux.py 同步编译标志
                       - 顺手修哈希缓存缺口：mtime 新鲜时未校验编译参数（本轮只改
                         编译标志、runtime.c 内容未变，导致对象不重编）；
                         现在 mtime 新鲜也会比对 sidecar（内容哈希+编译参数），
                         参数变更同样触发重编
                       - 效果：fib musl 288→177KB（运行时符号 160→10）；
                         probe-stdlib3 musl 297→192KB（符号 160→38）；
                         Windows probe-stdlib3.exe 约 30KB
                       - 验证：35 个 examples 全过（COFF 裁剪后入口/异常/闭包/
                         varargs 均正常）、51 测试全绿、六目标带新标志交叉编译通过、
                         aarch64 musl 链接通过

    2026-08-14 22:07  网络 TCP 基础（std/net，阻塞式）：
                       - 7 个函数：net_connect/net_send/net_recv/net_close/
                         net_listen/net_accept/net_port
                       - Windows WinSock2（WSAStartup 惰性初始化，链接补 -lws2_32）；
                         POSIX socket/getaddrinfo
                       - 排查并修复两个平台布局 bug：
                         a) sw_net_port 字节交换漏掩码：(port<<8)|(port>>8) 高字节
                            泄漏到 16-23 位（0x3DD0 被算成 0x3DD03D），
                            补 (port & 0xFF) 后正确；
                         b) addrinfo 布局：Linux/macOS 是 addr 在 24、canonname 在 32，
                            Windows 相反（canonname 24、addr 32），
                            按 _WIN32 排字段，否则 connect 读到 canonname 恒失败
                       - 新增 examples/probe-net.sw（纯回环：listen(0)→取端口→
                         connect/accept/send/recv/close + 拒绝连接测试），
                         36 个 examples 全过；cargo test 51 全绿；runtime.c 六目标
                         交叉编译通过；probe-net musl x86_64/aarch64 链接通过

    2026-08-14 22:17  数据模型补齐（第一批）：
                       - struct ==/!=：语义层把比较降级为按字段逐一比较（MIR 层），
                         字符串字段按内容（string_eq）、嵌套 struct 递归、其余标量按值；
                         支持与类型化字面量比较；空 struct 恒等
                       - 接口方法返回 struct：codegen 接口 vtable 间接调用补 sret
                         （签名首参 sret、返回 void、调用后返回地址）；
                         expr_owner_type 补 MirExpr::Struct（字面量可取字段）
                       - 闭包 struct 参数：移除语义层与 codegen 的限制
                         （struct 实参按地址传，间接调用一致）
                       - 已知限制（沿用）：对象字面量目标类型只在带注解声明/
                         嵌套字面量字段处传播（return/参数里需先赋给类型化变量）；
                         块体 lambda 返回类型恒为 void
                       - 新增 examples/probe-struct2.sw（8 项断言），37 个 examples
                        全过；51 测试全绿；probe-struct2 musl x86_64/aarch64 链接通过

    2026-08-14 22:26  数据模型补齐（第二批：类字段存 struct、struct 数组）：
                       - 类字段存 struct：class_layout 按基类优先展平、struct 值字段
                         内联其大小（对象头 8 字节 vtable）；new 按类总大小分配；
                         字段读写/构造/赋值走既有 struct 复制路径
                       - struct 数组：数组步长支持 struct 内联大小（sw_array 数据块
                         连续存放）；索引读返回元素地址、写走 copy_struct；
                         数组字面量按 struct 大小建槽并逐项复制；for-of 可用
                       - 语义层放开：class 字段允许 struct 值字段、struct 可赋给
                         类字段/数组元素、struct 数组字面量
                       - 新增 examples/probe-struct3.sw（11 项断言：类字段读写/赋值、
                         struct 数组索引/赋值/复制/for-of），38 个 examples 全过；
                         51 测试全绿；probe-struct3 musl x86_64/aarch64 链接通过
                       - 备注：重建 swc 后偶发首链接失败、重试即成功（环境抖动，
                         非代码问题）

    2026-08-14 22:34  内存管理强化：异常对象/帧纳入 GC + GC 压测：
                       - sw_try_begin/sw_throw 改用 sw_gc_alloc（异常帧与异常对象由
                         GC 回收，不再 malloc/free 泄漏；sw_current_frame 是数据段
                         全局根，出链后自然可回收）
                       - 修复隐藏 bug：catch 变量在 MIR 降级时未注册进名字作用域
                         （检查阶段注册了、降级阶段漏掉），catch(e) 里使用 e 报
                         "无法降级标识符"；降级时补插入作用域
                       - 新增 examples/probe-gc.sw：8 万次分配（字符串/数组/类/
                         struct/map）强制多次 GC 后数据完整；3 万次异常捕获循环
                         不泄漏不崩溃；异常后再分配正常
                       - 39 个 examples 全过；51 测试全绿；runtime.c 六目标交叉
                         编译通过；probe-gc musl x86_64/aarch64 链接通过

    2026-08-14 22:54  类继承 + 多文件引用（本批）：
                       - 类继承端到端：super() 调基类构造、基类字段/方法访问、
                         子类字段、方法覆盖全部通过（examples/probe-inherit.sw，
                         已注册进回归，退出码 0）
                       - 修复三个继承 bug：
                         a) super(...) 调基类构造漏传 this（MIR 降级 ExprKind::Super
                            分支补 MirExpr::Local(0)）；
                         b) check_member 字段目标错存「接收者类」而非「定义类」，
                            改为 FieldTarget::Class(class, index)（子类访问基类
                            字段偏移算错）；
                         c) ancestor_field_count 用 take_while 判定基类链恒返回 0，
                            改 skip(1) 统计基类字段数
                       - 多文件引用端到端（examples/probe-multifile.sw，退出码 0）：
                         import { Point, Vector, make_point } from "./lib-geo"；
                         跨文件 struct 字面量/字段、跨文件函数、跨文件类 new、
                         类字段/方法调用、struct 相等比较
                       - 修复三个跨模块 bug：
                         a) 跨模块 new 类构造：MirExpr::New 增加 sig 字段，
                            构造不在 exports 时按签名 declare_import 回退并补
                            self 参数；
                         b) expr_owner_type 补 MirExpr::Call 分支，识别函数返回
                            struct（p == make_point(...) 取字段被当 i64 的根因）；
                         c) 跨模块方法导入签名漏 self 接收者（call 实参 1、
                            导入签名 0 参数报错），callee_signature_for 的
                            MirCallee::Method 分支按 class 补前置 self 参数
                       - 回归：41 个 examples 全过；cargo test 51 全绿；
                         tools/run-examples.py 补 UTF-8 解码（Windows 控制台
                         GBK 读乱码报错）；cargo fmt 通过

    2026-08-14 23:07  macOS CI 修复（probe-stdlib2 disk_total + probe-net 超时）：
                       - disk_total/disk_free：macOS statvfs 的 fsblkcnt_t 是
                         uint32（Linux 是 uint64），原代码按 Linux 布局硬读 8 字节，
                         把 f_blocks 和 f_bfree 拼成天文数字导致 disk_total 断言
                         失败；按 __APPLE__ 分支读 4 字节（f_blocks@16、
                         f_bavail@24），frsize@8 不变
                       - probe-net 超时（120s）：探针某步失败后仍继续执行阻塞的
                         accept，直接卡死；改为每步失败立即 return 1，任何失败
                         都可在 CI 日志直接看到 [FAIL] 点，不再靠超时掩盖
                       - 顺手修 macOS sockaddr_in 布局：macOS 是 sin_len(1 字节)
                         + sin_family(1 字节)，端口在偏移 2；原结构体按 2 字节
                         family 定义，macOS 上 bind 会把 family 写进 sin_len、
                         sin_family 恒为 0；按 __APPLE__ 分支排字段并补 sin_len
                       - run-examples.py：超时也打印探针输出尾部，便于定位卡点
                       - 验证：runtime.c clang 交叉编译 aarch64-apple-darwin /
                         x86_64-linux 通过；Windows 41 个 examples 全过；
                         cargo test 51 全绿；提交推 Gitee + GitHub 复跑 CI

    2026-08-14 23:15  macOS probe-net 二次修复（net_connect 失败根因）：
                       - CI 日志明确：net_listen/net_port 通过、net_connect 失败
                       - 根因：addrinfo 字段顺序 macOS 与 Windows 一致
                         （canonname@24、addr@32），只有 Linux 是 addr@24、
                         canonname@32；原代码把 macOS 按 Linux 排，info->addr
                         实际读到 ai_canonname 字符串指针，connect 把 ASCII
                         当 sockaddr 用必然失败
                       - 修复：sw_addrinfo 按 _WIN32 / __APPLE__ / Linux 三分支
                         排字段；macOS 分支 addrlen 用 uint32（socklen_t）+
                         pad，保证 canonname@24、addr@32
                       - 验证：runtime.c 交叉编译 aarch64-apple-darwin /
                         x86_64-linux / x86_64-w64-windows-gnu 通过；
                         Windows 41 个 examples 全过；cargo test 51 全绿；
                         提交推 Gitee + GitHub 复跑 CI

    2026-08-14 23:38  跨文件全局变量 + 顶层变量解析修复 + 能力清单文档：
                       - 修 parser bug：顶层 let/const 声明解析完没消费结尾
                         `;`（函数体内语句路径有 expect(Semicolon)，顶层 item
                         路径漏了），`const VERSION = 3;` 一直报"顶层只能出现…";
                         parse_item 的 Variable 分支补 expect(Semicolon)
                       - 跨文件全局变量端到端（examples/lib-vars.sw +
                         probe-vars.sw，退出码 0）：
                         export const / export let，导入后读、跨模块写入共享；
                         - Symbol 增加 module 字段（声明归属模块）；
                         - MirGlobal 增加 module 字段；
                         - 语义层 lower_module 扫描本模块引用的跨模块 Global
                           符号，生成同名的 sw_global_<模块>_<名字> 导入条目
                           （支持 as 别名：用 decl_names 反查声明名）；
                         - codegen declare_global 按 module == 当前模块区分
                           Export（定义+初值）与 Import（外部数据），
                           读（MirExpr::Global）与写（MirTarget::Global）
                           都走既有路径
                       - 验证：import * as geo 命名空间别名导入可用
                         （examples/tmp-ns.sw，已手工验证）；42 个 examples
                         全过（新增 probe-vars）；cargo test 51 全绿
                       - 新增 docs/08-能力清单.md：实测能力清单（多文件导入
                         三种形式、全局变量/跨文件全局、跨文件引用表、
                         语言能力速览、已知限制），docs/README.md 挂索引
                       - 待确认：examples/tmp-vars-a~e.sw、tmp-ns.sw 为诊断
                         临时文件，是否删除待用户确认

    2026-08-14 23:56  综合功能案例 + 回归登记：
                       - 新增 examples/showcase.sw：可直接 swc run 的综合案例，
                         覆盖全局变量、类型别名、枚举声明、struct、泛型推导、
                         数组、循环、类/继承/接口、可空链、闭包、异常、模板字符串、
                         JSON、编码、时间、系统和路径 API
                       - tools/run-examples.py 登记 showcase.sw，端到端回归由 42 项增至 43 项
                       - 验证：cargo fmt --check、cargo test 51 项、43 个 examples 全通过
                       - 提交：1ca664e 新增综合功能示例

    2026-08-15 00:04  默认参数调用 + switch/break：
                       - MIR 降级在同模块顶层函数调用处补齐缺失的默认表达式，
                         add(4) 可按声明默认值调用
                       - switch 改为互斥的嵌套分支；case 内 break 合法并结束当前 case，
                         default 不再错误绑定到最后一个 case
                       - showcase 新增默认参数和 switch/case/break/default 运行断言
                       - 验证：cargo fmt --check、cargo test 51 项、showcase 通过
                       - 提交：a91322f 完善默认参数与 switch 控制流

    2026-08-15 00:33  继承类接口虚表修复：
                       - vtable 生成沿基类链收集接口；派生类现在继承基类接口的
                         方法地址，并可由自身覆盖方法替换实现
                       - 修复“派生类作为基类接口使用”会读取空函数指针、程序进入
                         main 前访问冲突的问题
                       - showcase 使用 InheritedCircle extends Circle（Circle implements Shape）
                         覆盖该组合路径
                       - 验证：cargo test 51 项、showcase 通过
                       - 提交：375c816 修复继承类接口虚表

    2026-08-15 00:40  复核用户批次 + 文档同步：
                       - 复核 1ca664e / a91322f / 375c816 / 5314458 四个提交：
                         Windows 本机 43 个 examples 全过（含 showcase）、
                         cargo test 51 全绿、cargo fmt 通过；showcase 16 组断言
                         全部 [ok]（类型/运算符/struct/数组/switch/默认参数/
                         类与接口/继承/可空链/闭包/泛型/字符串/编码/JSON/时间/
                         路径/异常/全局变量）
                       - 分析结论：a91322f 的 switch 降级改为嵌套互斥分支，
                         无 fall-through、default 兜底；break 在 switch 内合法；
                         默认参数在调用点按缺参补齐（仅同模块顶层函数）。
                         375c816 沿基类链收集接口槽位，修复派生类当基类接口
                         用时 vtable 空指针
                       - docs/08-能力清单.md 同步：固定宽度整数/浮点类型与
                         字面量后缀、switch 互斥语义、默认参数调用补齐、
                         继承接口虚表、showcase 综合示例入口
                       - 提交：本次（文档 + 进度复核记录）

    2026-08-15 00:58  trait 约束（第一档特性 A，Rust 风格）：
                       - 泛型参数 where 约束端到端：`function f<T>(x: T)
                         where T: Shape`——约束内可调接口方法（vtable 派发），
                         FunctionSig 增加 bounds 字段（参数名 → 接口类型列表），
                         build_function_sig 解析 where_clause 存入签名，
                         Checker 携带 bounds 上下文，check_call 的
                         TypeParam 分支按约束接口解析 CallTarget::InterfaceMethod
                       - 实例化校验：instantiate_generic 校验类型实参实现约束
                         接口（class_interfaces 查表），不满足报错
                       - 泛型类 implements 接口解锁（原 v0.1 显式报错）：
                         泛型类模板正常注册 class_interfaces，实例化时复制到
                         新实例 id；`const s: Shape = new Box<int>(7)`、
                         `s.area()` vtable 派发正常
                       - 限制：implements/where 约束目前只能是非泛型接口
                         （含 T 的接口约束报"目标必须是接口"）
                       - 新增 examples/probe-trait.sw（5 项断言：约束方法调用/
                         泛型类接口/虚表/字段类型替换），44 个 examples 全过；
                         cargo test 51 全绿；cargo fmt 通过

    2026-08-15 01:15  ADT 枚举 + match（第一档特性 B）：
                       - 语法：`enum Option<T> { Some(T), None }`（变体可带
                         字段）、`match (v) { Variant(x) => { ... }, _ => { ... } }`
                         （分支换行分隔，逗号可选；`_` 通配兜底）
                       - 类型：EnumInfo 升级为 EnumVariant（名称/判别值/字段），
                         支持泛型枚举 + 实例化缓存（TypeResolver::instantiate_enum +
                         Checker::instantiate_enum_types）；带字段变体 = ADT 枚举
                       - 检查：match 目标必须是枚举、模式变体存在、绑定数量/类型
                         校验、穷尽检查（缺变体报错，`_` 豁免）
                       - 构造：`Option.Some(42)`（有参，按实参推断泛型实参）、
                         `Option.None`（无参，带注解声明时经 enum_targets 目标
                         类型传播解析到实例；C 风格枚举 Color.Red 保持原语义）
                       - 表示：枚举对象 = GC 分配的 [tag, payload...] 槽
                         （tag@0、字段 @8*(i+1)），复用 sw_object_new；
                         MIR 增 EnumNew/EnumTag/EnumField（float 字段按 f64
                         位模式 bitcast），match 降级为 tag 比较嵌套 + 解构绑定
                       - 修复潜伏 bug：codegen local_types 曾把参数又拼一遍
                         （locals 已含参数），导致 index ≥ 参数个数的局部类型
                         错位——float 局部在含参函数里被当 int/枚举；改为直接
                         用 function.locals 建表（match 解构的 float 字段暴露）
                       - 新增 examples/probe-adt.sw（5 项断言：Some/None 匹配、
                         float 单字段、双字段、无字段变体），45 个 examples
                         全过；cargo test 51 全绿

    2026-08-15 01:21  Result + `?` 运算符（第一档特性 C，Rust 风格错误处理）：
                       - 标准库 std/result.sw：`enum Result<T, E> { Ok(T), Err(E) }`
                       - 语法：后缀 `expr?`（parser 仅在 `?` 后是表达式边界时
                         识别为 TryOp，避免与三元 `cond ? a : b` 冲突——修复
                         模板插值里三元表达式被误解析的副作用）
                       - 检查：`?` 目标必须含 Ok/Err 单字段变体；所在函数必须
                         返回 Result，且 Err 字段类型兼容；结果为 Ok payload 类型；
                         return/带注解声明的 Result 构造经 enum_targets 目标
                         类型传播实例化（此前 return 会推断出错误类型实参）
                       - 降级：MirExpr::TryPropagate——codegen 读 tag，Err 时
                         sw_object_new 构造函数 Result 的 Err 并 return，Ok 时
                         取出 payload（float 按位模式）
                       - 新增 examples/probe-result.sw（2 项断言：Ok payload、
                         Err 传播，含 match 消费），46 个 examples 全过；
                         cargo test 51 全绿

    2026-08-15 01:28  迭代器 + 数组切片（第二档 D+E）：
                       - 数组迭代器：`arr.map((x) => x * 2)`、`arr.filter((x) => x > 2)`
                         编译器内联循环（MirExpr::ArrayMap/ArrayFilter）：
                         sw_array_new 建新数组 + while 循环逐元素调闭包
                         （call_indirect），map 填结果、filter 按真值保留并
                         回写 len；支持链式 `.filter().map()`；检查端校验
                         函数参数类型与 filter 返回 bool
                       - 数组切片：`a[1:3]`、`a[:2]`、`a[2:]`（复制出新数组），
                         parser 先解析首表达式再判冒号；降级为
                         sw_array_slice(array, start, end)（缺省 0/len）；
                         runtime 实现越界裁剪
                       - 新增 examples/probe-iterator.sw（5 项断言）、
                         examples/probe-slice.sw（5 项断言），47 个 examples
                         全过；cargo test 51 全绿

    2026-08-15 01:31  @test 内嵌测试 + swc test（第二档 F）：
                       - `@test function name(): int`（0 通过/非 0 失败，也允许
                         void）；检查阶段校验无参数、返回类型
                       - 无 main 时语义层自动合成 sw_user_main runner：
                         逐个调用测试函数，非 0 记失败，拼接字符串经 println
                         输出 `[ok]/[FAIL] 函数名`，返回失败数
                       - CLI 新增 `swc test <file>`（同 run 流程，退出码=失败数）
                       - 新增 examples/probe-test.sw（3 个测试：算术/字符串/
                         match），负路径验证失败测试输出 [FAIL] 且退出码 1；
                         48 个 examples 全过；cargo test 51 全绿
                       - docs/08-能力清单.md 同步：泛型约束/泛型类接口/ADT+
                         match/Result+?/迭代器/切片/@test 与限制说明

    2026-08-15 01:35  泛型接口约束扩展（第一档延伸）：
                       - 泛型接口实例化：TypeTable 增 generic_interface_instances，
                         TypeResolver::instantiate_interface 替换方法签名类型实参；
                         Type::Interface 泛型实参（Container<int>）在 lower 时
                         实例化，where 约束/类型标注/extends 通用
                       - 非泛型类 implements 具体泛型接口实例：
                         class IntBox implements Container<int>
                       - 泛型类 implements 依赖类泛型参数的接口：
                         class Box<T> implements Container<T>——ClassInfo 增
                         implements（接口模板 id + 实参，可含 T），泛型类
                         实例化时替换实参并实例化接口注册 vtable
                       - where T: Container<int> 约束 + 约束内方法调用 +
                         实例化校验（class_interfaces 查表）全部打通
                       - 新增 examples/probe-generic-iface.sw（5 项断言：
                         具体/泛型类实现泛型接口、约束调用、vtable get/set），
                         49 个 examples 全过；cargo test 51 全绿

    2026-08-15 01:44  @test 断言库 + 标准库扩充 + 循环 import 支持：
                       - std/test.sw：assert(cond[, msg])（重载）、
                         expect_eq（int/float/string/bool/char 重载）、
                         expect_true/expect_false/fail；失败抛异常
                       - 测试 runner 加 try/catch（sw_setjmp/sw_try_begin/
                         sw_try_value/sw_try_leave 每测试一个异常框架）：
                         断言抛出的异常被捕获并记为 [FAIL]，不再崩溃整个进程
                       - 标准库扩充：std/string +6（remove_prefix/remove_suffix/
                         is_upper/is_lower/is_digit/capitalize，全部注册链式
                         方法）；std/array +6（contains_int/float/string、
                         index_of_int/float/string）
                       - 修复循环 import：模块加载/绑定与 finalize/检查/降级
                         拆成两阶段（先全量加载绑定，再逆序 finalize——被依赖
                         模块后加载先解析类型——最后统一检查降级）；此前循环
                         依赖下被引用函数签名停留在占位导致调用解析失败
                       - 双向调用演示：examples/lib-a.sw ↔ lib-b.sw
                         （循环 import 互调）+ probe-circular.sw（a 调 b、
                         b 调 a、跨文件函数）
                       - 新增 probe-stdlib4.sw（17 项断言）、probe-test.sw
                         改用断言库（3 测试）、tmp-test-assert-fail.sw 验证
                         断言失败路径；51 个 examples 全过；cargo test 51 全绿

    2026-08-15 01:53  expect_eq 数组/struct 比较 + 跨模块泛型实例化 + CI 接入：
                       - std/test 的 expect_eq 改为泛型 expect_eq<T>（标量/
                         struct/可空按值比较，struct 走逐字段值比较）+ 3 个
                         数组重载（int[]/float[]/string[] 长度+逐元素比较）
                       - 跨模块泛型函数实例化解锁：instantiate_generic 不再
                         限制本模块，模板函数体从 all_modules 按模块+span 取；
                         关键修复：
                         a) FnLower 增加 result_index，检查结果表（ident_
                            symbols/expr_types/call_targets 等）按模板所属
                            模块取，跨模块实例化时模板 body 的 span 能查到
                            参数/类型信息；
                         b) expr_type 对 TypeParam 用 type_args 替换
                            （实例化后的具体类型）；
                         c) analyze 顺序拆成 finalize 全部 → check 全部 →
                            lower 全部（此前 check/lower 逐模块，调用点模块
                            降级时模板模块的检查结果还是空的）
                       - CI 三平台 Smoke 增加 `swc test examples/probe-test.sw`
                       - probe-test 扩为 5 个测试（数组/struct 比较），
                         51 个 examples 全过；cargo test 51 全绿

    2026-08-15 02:03  文档：新增 docs/项目说明.md（项目定位/仓库/约定/当前
                       进度/验证现状/常用命令/已知限制/待办），供新窗口
                       快速接手上下文

    2026-08-15 02:20  标准库扩充（std/map +1）：
                       - 新增 map_values(map)：返回全部值的 string[]，与 map_keys
                         同序（插入顺序）；runtime 加 sw_map_values（遍历节点收集
                         值，模式同 sw_map_keys）；stdlib/map.sw 声明 + codegen
                         extern_c_symbol 映射
                       - 新增 examples/probe-map-values.sw（6 项断言：key/value
                         顺序对应、覆盖更新后取值、空 map），注册进 run-examples.py
                       - 验证：cargo test 51 全绿、showcase 通过、probe-map-values 通过

    2026-08-15 02:25  修复 u8[] 切片数据损坏 bug：
                       - 根因：sw_array_slice 硬编码 8 字节元素步长（原实现
                         start*8 / count*8），而 u8[] 是紧凑布局（步长 1），
                         对 u8[] 切片错位复制 → 元素全变 0
                       - 修复：sw_array_slice 增加 elem_size 参数（第 4 参），
                         按 elem_size 做偏移与 memcpy；语义层 slice 降级按对象
                         元素类型传步长（U8→1、其余→8）；codegen 签名扩为 4 参
                       - 复现：examples 下临时探针（[1u8,2u8,..6u8][1:4] 原输出
                         全 0，修复后 [2,3,4]）
                       - 新增 examples/probe-slice-u8.sw（5 项断言：u8 中/头/尾
                         切片、原数组 intact、int 切片不回归），注册进
                         run-examples.py
                       - 验证：cargo test 51 全绿、showcase/probe-slice/
                         probe-array 无回归、probe-slice-u8 通过

    2026-08-15 02:30  泛型约束剩余项（其一）：where 约束校验沿基类链查：
                       - 修复不一致：instantiate_generic 校验 where 约束时只查
                         实参类自己的 class_interfaces，但 vtable 生成与
                         Class→Interface 赋值都沿基类链收集——接口若只被基类
                         implements（子类未写），fn f<T>(x: T) where T: Shape
                         传子类实参会误报"未实现约束接口"
                       - 修复：校验改为沿 class_base_chain 收集接口（symbols.rs
                         class_base_chain 已存在），与 vtable/赋值规则对齐
                       - 新增 examples/probe-bound-inherit.sw（3 项断言：基类满足
                         约束、派生类经基类继承满足约束、派生类可赋给基类接口），
                         注册进 run-examples.py
                       - 验证：53 个 examples 全过（含新探针，设计退出码不变）、
                         cargo test 51 全绿、showcase 通过

    2026-08-15 02:35  泛型约束剩余项（其二）：泛型接口约束缺实参显式报错：
                       - 修复静默退化：`where T: Container`（泛型接口缺 `<int>`）
                         build_function_sig 的 lower 会把约束退化成模板 id，导致
                         约束校验永不匹配、方法返回类型残留 TypeParam，报出
                         误导性的"未实现约束接口"
                       - 修复：finalize 阶段对顶层函数 where_clause 前置校验——
                         约束目标是泛型接口且无类型实参时，显式报"泛型接口约束
                         必须带类型实参（如 `where T: Container<int>`）"
                       - 单测：semantic.rs 新增 reports_generic_interface_bound_
                         without_type_args（断言该诊断存在）；18 项语义测试全绿
                       - 验证：cargo test 51 全绿；53 examples 通过（合法用法
                         where T: Container<int> / where T: Shape 无误伤）

    2026-08-15 02:40  泛型约束剩余项（其三，进行中）：where 约束里接口实参为类型
                       参数（`where T: Container<U>`）的反向推导：
                       - 检查期：pick_overload 增 derive_bound_params_checker——从实参类
                         实现的同模板接口具体实例反推 U 写入 type_args（使泛型返回
                         类型能被替换）；instantiate_generic 降级期同样反推并缓存
                       - 修 TypeParam 泄漏：FnLower::expr_type 对记录类型做 substitute_type
                         （约束接口方法返回的 U 在实例化时替换）；MirCallee::
                         InterfaceMethod 的方法签名也同样替换 type_args
                       - 效果：`where T: Container<U>` 现在能通过语义检查并编译
                         （此前报"未实现约束接口"/"不支持类型 U"）
                       - 未完成：接口约束派发运行时的 vtable 槽位对齐待修——bound 接口
                         用 TypeParam 实例化的 id 与实参类具体接口 id 不一致，运行时
                         read_via(box) 在 vtable 读取处崩溃（0xC0000005）。此路径尚未
                         生成正式探针
                       - 验证：cargo test 51 全绿、53 examples 全部通过（无回归）；
                         反向推导使新场景编译通过但运行时待修

    2026-08-15 02:50  泛型约束剩余项（完成）：where T: Container<U> 反向推导端到端：
                       - 接 02:40 进行中的 vtable 运行时槽位对齐：
                         MirCallee::InterfaceMethod 派发时分发用 bound 接口（含
                         TypeParam 实参的实例 id）算 vtable 槽位，而实参类 vtable 注册的是
                         具体接口实例 id → 槽位错位、运行时读野指针崩溃（0xC0000005）
                       - 修复：FnLower::resolve_concrete_interface——把 bound 接口 id 经
                         type_args 替换实参后查 generic_interface_instances，得到实参类
                         实际注册的接口实例 id 用于派发；非泛型接口/未解析参数退回原 id
                       - 效果：`where T: Container<U>` 反向推导全链路可用（此前运行到
                         read_via(box) 即崩）
                       - 新增 examples/probe-bound-reverse.sw（4 项断言：U=int / U=string
                         反推 + vtable get/set、非泛型约束回归），注册进 run-examples.py
                       - 验证：cargo test 51 全绿、examples 全部通过
                       - 完成 02:40 的"反向推导编译器阶段"闭环

    2026-08-15 03:00  修复纯 C 风格枚举（无字段变体）成员访问损坏：
                       - 实测复现：`enum Color { Red, Green, Blue }` 的 `Color.Red`
                         报"成员访问未解析"（语言研读发现）
                       - 根因：check_member 的 EnumName.Variant 分支被 `成员任一带
                         字段`门控，纯 C 风格枚举（全空变体）不满足，落入把类型名
                         当值解析的错误路径
                       - 修复：去掉"任一成员带字段"门控，枚举类型名成员访问一律登记
                         EnumConstruct（tag=discriminant，变体不存在才报错）
                       - 新增 examples/probe-enum-c.sw（C 风格枚举构造+match、ADT
                         枚举回归），注册进 run-examples.py
                       - 验证：cargo test 51 全绿、probe-enum-c / probe-adt /
                         probe-result / showcase 通过

    2026-08-15 03:10  标准库扩充：std/unicode 3→7 个函数：
                       - utf8_byte_len(text)：UTF-8 字节数
                       - utf8_index_to_byte(text, char_index)：字符序号→字节偏移（越界 -1，末尾=len）
                       - utf8_byte_to_index(text, byte_offset)：字节偏移→字符序号（落在多字节中间 -1）
                       - utf8_is_printable(text)：是否全为可打印字符（排除控制符/私有区段）
                       - runtime.c 用现有 sw_utf8_char_length/sw_utf8_decode 实现，plain 链接
                       - 新增 examples/probe-unicode.sw（13 项断言）注册进 run-examples.py
                       - 验证：cargo test 51 全绿、probe-unicode 通过

    2026-08-15 03:20  标准库扩充：std/string 新增 parse_bool：
                       - string 转 bool：true/1/yes → true，false/0/no → false（不区分
                         大小写），无效 → false；并注册进 string_method 链式方法表
                         （"YES".parse_bool()）
                       - runtime.c 加 parse_bool + 辅助 sw_ascii_lower/sw_text_equals_ci，
                         plain 链接
                       - 新增 examples/probe-bool.sw（10 项断言）注册进 run-examples.py
                       - 验证：cargo test 51 全绿、probe-bool 通过

    2026-08-15 03:30  标准库扩充：std/array 新增降序排序（+3）：
                       - sort_int_desc / sort_float_desc / sort_string_desc（原地降序）
                       - runtime.c 加 sw_cmp_i64_desc/f64_desc/str_desc 比较器 +
                         sw_sort_{int,float,string}_desc；codegen extern_c_symbol 映射 3 项
                       - 新增 examples/probe-array-desc.sw（5 项断言）注册进 run-examples.py
                       - 验证：cargo test 51 全绿、probe-array-desc 通过

    2026-08-15 03:40  标准库扩充：std/map 新增 map_clear（8→9）：
                       - map_clear(map)：清空所有键值（重置 head/tail/count，节点由 GC 回收）
                       - runtime.c 加 sw_map_clear；codegen extern_c_symbol 映射 1 项
                       - 新增 examples/probe-map-clear.sw（5 项断言：清空/复用/再 set/keys）
                         注册进 run-examples.py
                       - 验证：cargo test 51 全绿、probe-map-clear 通过

    2026-08-15 03:50  标准库扩充：新增 std/hash 模块（FNV-1a / DJB2）：
                       - fnv1a_64(text)：64 位 FNV-1a
                       - fnv1a_64_seed(text, seed)：带自定义种子
                       - djb2(text)：DJB2 字符串哈希
                       - runtime.c 加 fnv1a_64/fnv1a_64_seed/djb2，plain 链接
                       - 新增 examples/probe-hash.sw（7 项断言：确定性/种子/大小写/空串）
                         注册进 run-examples.py
                        - 验证：cargo test 51 全绿、probe-hash 通过

    2026-08-15 02:41  复核用户批次 + 全量验证 + 提交：
                       - 学习用户新增的 10 个提交：
                         a) 泛型约束剩余项：where T: Container<U> 反向推导
                            （从实参类实现的同模板接口实例反推 U）、vtable
                            槽位对齐修复（resolve_concrete_interface 经
                            type_args 替换查具体接口实例 id）、泛型接口约束
                            缺实参显式报错、约束校验沿基类链
                         b) C 风格枚举成员访问修复（去掉"任一带字段"门控，
                            EnumName.Variant 一律登记 EnumConstruct）
                         c) 标准库：std/hash（fnv1a_64/seed/djb2）、
                            map_values/map_clear、sort_*_desc 降序、
                            parse_bool（链式）、unicode +4
                            （utf8_byte_len/index_to_byte/byte_to_index/
                            is_printable）、u8[] 切片 elem_size 修复
                       - 全量验证：61 个 examples 全过、cargo test 52 全绿
                         （含新增 semantic 测试 1 项）
                       - probe-test.sw 格式化：@test 独占一行（确认属性
                         跨行写法等价）
                       - 提交：本次（probe-test 格式 + 进度复核记录）

    2026-08-15 02:45  修复 CI 三平台 fmt 失败：
                       - GitHub CI 三平台 cargo fmt --check 全挂：用户新提交
                         的泛型约束代码（check.rs 829/5568）与 semantic 测试
                         （tests/semantic.rs 478/486）未按 rustfmt 格式化
                       - 本地 cargo fmt --all 修复 2 个文件（16+/8-），
                         fmt --check 通过；61 examples、52 测试全绿
                       - 提交推 Gitee + GitHub 复跑三平台 CI

    2026-08-15 02:56  match 表达式（A1，返回值形式）：
                       - 语法：`const x = match (v) { Some(n) => n, None => 0 };`
                         分支体为表达式（与语句版 match 分支体为块区分），
                         分支间逗号可选，`_` 通配兜底
                       - AST：ExprKind::MatchExpr + MatchExprArm；parser 在
                         parse_primary 增加 match 表达式解析
                       - 检查：枚举校验、模式绑定作用域、穷尽检查、分支类型
                         合并（可赋值双向，与三元一致）
                       - MIR：MirExpr::MatchExpr + MatchArmMir（tag/绑定/体）；
                         降级复用语句 match 的解构绑定逻辑，绑定变量进
                         name_scopes；codegen 生成 dispatch 链（tag 比较 →
                         分支块解构绑定 + 求值存结果槽 → join 读回），
                         float 分支按 F64 位模式
                       - 新增 examples/probe-match-expr.sw（6 项断言：字符串/
                         float/双绑定/无字段变体/int 表达式），62 个 examples
                         全过；cargo test 52 全绿；fmt 通过

    2026-08-15 03:16  JS 语法补齐批次（A2：逻辑赋值/展开/解构/数组方法）：
                       - 逻辑赋值：`&&=`、`||=`（token/lexer/AssignOp/降级
                         为 Binary And/Or，目标要求 bool）
                       - 展开运算符：
                         a) 数组字面量 `[...a, 99, ...b]`——MirExpr::ArraySpread，
                            codegen 动态总长 + 按原始顺序填充（固定项 +
                            spread 内层循环复制，u8/int/float/string；struct
                            元素暂不支持 spread）
                         b) struct 字面量 `{ ...origin, y: 9 }`——展开为
                            Field 表达式，显式字段覆盖
                         c) 调用 `f(...[1, 2, 3])`——数组字面量展开为多个
                            实参（变量数组展开暂不支持），varargs 位置也能
                            展开打包
                       - 解构声明：`const [a, b] = arr;`、
                         `const { x, y } = p;`、`const { x: renamed } = p;`
                         ——init 只求值一次（临时局部），绑定按 Index/Field
                         生成 VarDecl；数组解构支持函数调用返回值
                       - 数组方法：push/pop（运行时 sw_array_push/pop，
                         扩容复制）、forEach/some/every/find（MirExpr::
                         ArrayIterate 内联循环，some/every 提前退出，find
                         返回可空元素，找不到 null）
                       - 修复潜伏 bug：codegen 临时槽（new_slot）与局部变量
                         槽（slot_for）共用 slots 表，数组/map/match 等临时
                         槽会错位后续 struct 局部变量（暴露于展开测试）；
                         分离 temp_slots
                       - 新增 probe-spread.sw（8 断言）/probe-destructure.sw
                         （4 断言）/probe-array-methods.sw（8 断言），
                         65 个 examples 全过；cargo test 52 全绿；fmt 通过
                       - 待办：class static 方法/字段（parser 已解析 static
                         关键字，语义层未实现）

    2026-08-15 03:23  class static 方法/字段：
                       - 语法：`static field: int;`、`static method(...)`
                         （parser 方法分支把 Static 修饰符写入 FunctionDecl.static_）
                       - 语义：static 字段不占实例内存（ClassInfo 分离
                         static_fields/static_methods，finalize_class_members
                         按修饰符分流；实例字段/方法/method_index 不受影响）
                       - 实现：static 字段 → 模块级 MirGlobal（sw_sfield_<类>_<idx>，
                         static_field_globals 表）；static 方法 → 独立
                         MirFunction（sw_smethod_<类>_<idx>_<名>，无 this）
                       - 访问：`Class.field`（check_member 识别类型名 → 静态
                         成员，降级 MirExpr::Global）、`Class.method(args)`
                         （check_call → CallTarget::StaticMethod → 直接函数调用）；
                         check_all_functions 对 static 方法用 static_methods
                         签名检查（this_class=None）
                       - 限制：static 字段 v0.1 只支持标量（int/float/bool），
                         引用类型（string/数组）默认 null 会崩；static 字段
                         暂不支持写入（读 + 方法调用可用）
                       - 新增 examples/probe-static.sw（4 断言），66 个
                         examples 全过；cargo test 52 全绿；fmt 通过

    2026-08-15 03:31  编译产物类型 + swcc.toml（console/dll/lib）：
                       - swcc.toml 极简解析（无外部依赖，手写）：向上查找
                         入口文件目录；[build] kind = "console"|"dll"|"lib"、
                         [lib] name 决定产物名
                       - lib：llvm-ar rcs 打包 Sw 模块对象（不含运行时，
                         使用方自行链接）；Windows .lib、Linux/macOS .a；
                         验证 llvm-ar t 含 sw_fn_* 导出符号
                       - dll：Windows --dll + --export-all-symbols +
                         --out-implib（导入库）、Linux -shared、macOS
                         -dynamiclib；runtime 编译加 -DSW_NO_MAIN（不生成
                         main，Windows 提供 DllMainCRTStartup 入口，跳过
                         exe startup 对象）；产物 .dll/.so/.dylib + 导入库
                       - 验证：Windows 本机 swmath.dll（97KB）+ 导入库
                         （48KB）生成，导出 DllMainCRTStartup/sw_fn_*；
                         C 程序链接导入库调用 sw_fn_0_add_7 输出 42；
                         swmath.lib（静态库）打包验证
                       - 限制：dll 导出名为 Sw stable 符号（sw_fn_*），
                         用户友好名映射（.def）列入待办；lib 不含运行时
                       - 66 个 examples 全过（console 无回归）；cargo test
                         52 全绿；fmt 通过

    待办（下一批，按推荐优先级）：
       1. 泛型类 implements 接口；Windows aarch64；调试信息/LSP
       2. 标准库继续扩充：std/io 补实用函数（或用纯 Sw 包 format 做 print_format）
          或 regex 模块

二、已知限制

- Windows aarch64（COFF）暂不支持：Cranelift 对象写入器未实现 AArch64 COFF 重定位
- 单线程：异常帧栈为全局变量，多线程留待后续版本
- 调试信息与错误诊断体验待完善（v0.2）
- struct 嵌套/struct 数组/类字段存 struct 暂不支持（v0.1 显式报错）
- 浮点全局变量未验证（全局按 64 位常量槽存储）
- GC 为保守式：异常帧/抛出对象暂不参与回收，高频抛异常可能累积少量内存
- 泛型仅支持本模块函数实例化；泛型 struct/class 暂未支持
- Windows aarch64（COFF）仍受 Cranelift 对象写入器限制

三、下一批候选（待排期）

1. 语言完整度：泛型 struct/class、枚举/接口后端验证、解构/可选链等语法
2. 标准库扩充：JSON、进程、网络、时间；字符串按字符（Unicode 标量）语义
3. 内存管理进阶：GC 压测调优（阈值自适应）、异常对象纳入 GC、`weak<T>` 弱引用
4. 开发者体验：错误诊断美化、格式化器、LSP、调试信息
5. 并发：多线程运行时与线程安全的异常机制
6. 发布：0.1.0 三平台产物、安装包/更新通道
